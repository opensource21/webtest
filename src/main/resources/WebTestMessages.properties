# Argument 0 event-source
# Argument 1 group
# Argument 2 item
# Argument 3 action
# Argument is the display-name of the the test.
test.start=Start test {4}.
test.success=Finish test {4} successful.
test.skipped=Test {4} skipped.
# Arguments are the display-name of the the test and number of test with failures, .
test.failures=Test {4} finished, with {5} failure(s).
# Arguments are the display-name of the the test and number of test with failures, .
test.exception_occurs=Test {4} has exception: {5}.
test.exception=Test {4} canceled with following exception: {5}.

# Argument the assertion-error.
assertion.failed={4}

# Argument session-id
webdriver.create_instance=Create new webdriver-instance with session-id >{4}<.

# Argument url.
webdriver.get=Call {4}.

# Element Argument Arguments.
element.click=Click on {2} on Page {1}
element.sendKeys=Send {4} to {2} on Page {1}
element.submit=Submit {2} on Page {1}
# 4 Pagename, 5 FieldDescription 6 Attributename, 7 Attribute-Value
element.getAttribute=Get Attribute >{6}< from >{5}< on Page >{4}<:{7}
